<div class="container">

<div class="col-xs-9">

  <div class="main-visual">
    <h4 class="text-center">ようこそ、ながのCAKEへ！<br>
    このサイトは、ケーキ販売のECサイトになります。<br>
    会員でない方も商品の閲覧ができますが、<br>
    購入には会員登録が必要になります。</h4>
  </div>

  <h1>新着商品</h1>
  <div class="new-items-wrapper">


    <% @items.last(4).each do |item| %>
        <div class="item-box">
          <table class="table table-bordered">

            <tr>
              <th><%= link_to public_item_path(item) do %>
                <%= attachment_image_tag(item, :image,size: "180x130", fallback: "no_image.jpg") %>
                <% end %>
              </th>
            </tr>
            <tr>
              <th><%= link_to item.name, public_item_path(item) %></th>
            </tr>
            <tr>
              <th>￥<%= (item.price * 1.1).to_i %></th>
            </tr>
          </table>
        </div>
    <% end %>
  </div>

  <div>
    <%= link_to "全ての商品を見る>", public_items_path %>
  </div>
  </div>
</div>
