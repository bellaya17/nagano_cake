<div class="container">
  <h1>商品詳細</h1>
    
    <%#= image_tag @item.image,  class:"single-image" %>
    
    <h4>商品名</h4>
    <%= @item.name %>
    
    <h4>商品説明</h4>
    <%= @item.introduction %>
    
    <h4>ジャンル</h4>
    <%= @item.genre.name %>
     
    <h4>税込価格 <br>(税抜価格）</br></h4>
    <%= @item.price %>
     
    <%= link_to "編集する", edit_admin_item_path(@item), class:"btn btn-success"%>
</div>
